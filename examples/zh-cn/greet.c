#include <stdio.h>

int main(int argc, char **argv) {
    // 待办: 验证 1 < argc < 3
    printf("Program: %s\n", argv[0]);
    printf("Hello, %s!\n", argv[1]);

    // 待办
    // 提供语义退出代码
    // return 0;
}
