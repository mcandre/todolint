#include <stdio.h>

int main(int argc, char **argv) {
    // 待辦: 驗證 1 < argc < 3
    printf("Program: %s\n", argv[0]);
    printf("Hello, %s!\n", argv[1]);

    // 待辦
    // 提供語意化退出碼
    // return 0;
}
